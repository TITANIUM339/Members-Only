<header>
    <div class="logo">
        <a href="/">Members <span>Only</span></a>
    </div>
    <nav>
        <ul>
            <% if (locals.user) { %>
                <li class="nav-item">
                    <form action="/log-out" method="POST">
                        <button type="submit">Log out</button>
                    </form>
                </li>
            <% } else { %>
                <li class="nav-item">
                    <a href="/log-in">Log in</a>
                </li>
                <li class="nav-item">
                    <a href="/sign-up">Sign up</a>
                </li>
            <% } %>
        </ul>
    </nav>
</header>
